# Contact links

Format:

```
hashchat://contact/v1/<v3-onion>/<64-hex-x25519>
```

Public only. Generated by `:my-contact` after `:listen`. Parsed by `:add-contact`.

Both sides compute `X25519(our_static, their_static)` and `init_symmetric`. Later frames carry an ephemeral DH public (wire v2).
