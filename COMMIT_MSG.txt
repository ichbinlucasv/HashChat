docs(desktop): Rust-only recommended path; demote Haskell

Advance Haskell retirement without deleting the tree:
- run-tui / install*.sh never prefer cabal when cargo can build;
  Haskell opt-in only via HASHCHAT_ALLOW_HASKELL=1
- build.sh Rust-first; Cabal only with --haskell
- Makefile target tui → cargo release build --features tui
- README/INSTALL/CONTRIBUTING/ROADMAP: Haskell desktop transitional /
  not recommended; document removal criteria in INSTALL.md

cargo test --lib: 51 passed. No push.
