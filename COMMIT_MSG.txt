feat(persist): durable NetConfig (mode/dns/posture) in session blob v3

Persist network prefs inside the encrypted session wrap so :mode /
:mode extreme survive restart. Load still accepts v1/v2 (defaults);
save always writes v3. Env seeds cold start / new identity; blob wins
after unlock. Extreme Tor-only gating kept fail-closed for listen,
send, and :my-contact.
